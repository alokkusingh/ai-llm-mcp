spring:
  application:
    name: llm-mcp-realworld-server

  ai:
    mcp:
      server:
        name: realworld-tools-server
        version: 1.0.0
        type: SYNC
        stdio: false
        base-url: /api/v1
        sse-endpoint: /sse
        sse-message-endpoint: /mcp/messages

server:
  port: 8088